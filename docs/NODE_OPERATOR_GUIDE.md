# Node Operator Guide

## API Endpoints

The following API endpoints are available:

* `/api/network`: Returns information about the network.
* `/api/peers`: Returns a list of connected peers.
* `/api/agents`: Returns a list of agents.
* `/api/bounties`: Returns a list of available bounties.

Note: These endpoints are currently not implemented and will return a 404 error.