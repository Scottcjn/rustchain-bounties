```json
{
  "type": "code",
  "language": "Rust",
  "code": "// test_corpus_and_fuzz.rs\n// Bounty: 25 RTC — Test corpus + fuzz harness for node x86/device arch classification\n// Armor around derive_verified_device — NOT the core fix\n\nuse std::collections::HashMap;\nuse rand::Rng;\nuse serde::{Deserialize, Serialize};\n\n/// Simulated device classification input structure\n#[derive(Debug, Clone, Serialize, Deserialize)]\npub struct DeviceInput {\n    pub cpu_arch: String,\n    pub features: Vec<String>,\n    pub entropy_profile: HashMap<String, u64>,\n    pub legacy_keys: Vec<String>,\n}\n\n/// Classification output\n#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\npub enum DeviceClass {\n    X86Vintage,\n    X86Modern,\n    ArmVintage,\n    ArmModern,\n    Unknown,\n}\n\n/// Target function for fuzzing (simulated derive_verified_device)\npub fn derive_verified_device(input: &DeviceInput) -> DeviceClass {\n    let arch_lower = input.cpu_arch.to_lowercase();\n    \n    // Detect x86 family\n    let is_x86 = arch_lower.contains(\"x86\") || arch_lower.contains(\"amd64\") || arch_lower.contains(\"intel\");\n    let is_arm = arch_lower.contains(\"arm\") || arch_lower.contains(\"aarch64\");\n    \n    // Check legacy keys (the bug: entropy profile reads legacy key names)\n    let has_legacy_keys = input.legacy_keys.iter().any(|k| {\n        input.entropy_profile.contains_key(k)\n    });\n    \n    // Vintage detection based on features and entropy\n    let is_vintage = input.features.contains(&\"vintage\".to_string()) \n        || input.features.contains(&\"legacy\".to_string())\n        || has_legacy_keys;\n    \n    // Modern detection\n    let is_modern = input.features.contains(&\"modern\".to_string())\n        || input.features.contains(&\"neon\".to_string())\n        || input.features.contains(&\"avx2\".to_string());\n    \n    match (is_x86, is_arm, is_vintage, is_modern) {\n        (true, _, true, _) => DeviceClass::X86Vintage,\n        (true, _, fal