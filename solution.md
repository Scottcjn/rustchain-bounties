```json
{
  "type": "code",
  "language": "rust",
  "code": "// tests/integration_test.rs\n// Integration test suite for clawrtc-rs\n\nuse clawrtc::{Wallet, Address, Attestation, Error};\nuse ed25519_dalek::Keypair;\nuse rand::rngs::OsRng;\n\n// Helper to create a test wallet\nfn create_test_wallet() -> Wallet {\n    let mut csprng = OsRng {};\n    let keypair: Keypair = Keypair::generate(&mut csprng);\n    Wallet::from_keypair(keypair)\n}\n\n// 1. Wallet roundtrip: generate → serialize → deserialize → sign → verify\n#[test]\nfn wallet_roundtrip() {\n    let wallet = create_test_wallet();\n    let message = b\"Test message for roundtrip\";\n    \n    // Sign\n    let signature = wallet.sign(message).expect(\"Signing failed\");\n    \n    // Verify with original wallet\n    assert!(wallet.verify(message, &signature).is_ok());\n    \n    // Serialize and deserialize\n    let serialized = wallet.to_bytes();\n    let deserialized = Wallet::from_bytes(&serialized).expect(\"Deserialization failed\");\n    \n    // Verify with deserialized wallet\n    assert!(deserialized.verify(message, &signature).is_ok());\n    \n    // Ensure addresses match\n    assert_eq!(wallet.address(), deserialized.address());\n}\n\n// 2. Address vectors: deterministic address generation\n#[test]\nfn address_determinism() {\n    let wallet1 = create_test_wallet();\n    let wallet2 = Wallet::from_bytes(&wallet1.to_bytes()).expect(\"Clone failed\");\n    \n    let addr1 = wallet1.address();\n    let addr2 = wallet2.address();\n    \n    assert_eq!(addr1, addr2, \"Addresses should be deterministic from same keypair\");\n    \n    // Address format validation (assuming Address implements Display)\n    let addr_str = format!(\"{}\", addr1);\n    assert!(!addr_str.is_empty(), \"Address string should not be empty\");\n    assert!(addr_str.len() >= 10, \"Address should be reasonably long\");\n}\n\n// 3. Attestation determinism: same input yields same attestation\n#[test]\nfn attestation_determinism() {\n