Implemented on branch `earnbot/sec-Scottcjn-rustchain-bounties-1112`.

- Commit: `f08fb56`
- Files changed:
  - [scripts/stress_test/fuzz_attest_submit.py](/Users/zheliu/Documents/Codex/2026-05-11/now-go-out-and-make-at-2/data/work/Scottcjn__rustchain-bounties/scripts/stress_test/fuzz_attest_submit.py)

What this changes:
1. Adds a dedicated endpoint fuzzing runner for `POST /attest/submit`.
2. Generates at least 100 deterministic malformed/adversarial cases across:
   - missing fields
   - wrong types
   - oversized inputs
   - injection-style payloads (including raw invalid JSON)
3. Fetches fresh nonce per case from `/attest/challenge`.
4. Emits structured JSON + Markdown reports with per-category and per-class response outcome summaries.
5. Explicitly classifies results into:
   - handled validation failures (4xx)
   - server-side faults (5xx)
   - transport errors

Verification run:
1. Command run:
   - `python3 scripts/stress_test/fuzz_attest_submit.py --count 120 --concurrency 4 --timeout 20 --out-json /tmp/fuzz_results_1112.json --out-md /tmp/fuzz_report_1112.md`
2. Summary from that run:
   - Total cases: 120
   - Handled validation: 120
   - Server-side faults (5xx): 0
   - Transport errors: 0
   - Status distribution: `400: 16`, `422: 2`, `429: 102` (plus no `2xx/5xx` outcomes beyond those)
3. Reproducible run outputs are written to `/tmp/fuzz_results_1112.json` and `/tmp/fuzz_report_1112.md` when needed.

Security-impact one-liner:
- `/attest/submit` appears to fail malformed/adversarial requests in expected non-5xx classes (validation/rate-limit responses), with no server-side crashes/exceptions observed in this 120-case run.