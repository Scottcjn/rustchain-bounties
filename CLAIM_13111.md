# Code Review Bounty Claim #13111

**Claimant:** o1TIMI
**PR Reviewed:** https://github.com/Scottcjn/Rustchain/pull/6857
**Review Submitted:** https://github.com/Scottcjn/Rustchain/pull/6857#pullrequestreview-4431778633
**Bounty:** #73

## Summary

First substantive review of PR #6857. The new checksum regeneration helper (`scripts/regenerate_miner_checksums.sh`) was found to have two issues:

1. **CRLF line ending bug:** When `miners/checksums.sha256` has CRLF line endings (common on Git Bash checkouts on Windows), the `\r` character remains in the artifact path, causing the checksum verification to fail.
2. **Hardcoded `sha256sum`:** The script hardcodes `sha256sum` despite being documented for Linux/macOS miner contributors, which may not be available on all systems.

## Validation

- `python -m pytest -q tests/test_install_miner_checksums.py` → 2 passed
- `git diff --check` → passed
- `./scripts/regenerate_miner_checksums.sh` under Git Bash → failed with CRLF path issue

## Wallet

`RTCe0961d6b54f2fa96db57a373c84d8ad8986153f8`

## Notes

No payout is asserted unless/until maintainers verify and accept the review claim.
