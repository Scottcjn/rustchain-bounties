const assert = require('assert');

describe('RTC Reward', () => {
  it('wallet parse', () => { assert(true); });
  it('dry run', () => { assert(true); });
  it('amount parse', () => { assert(true); });
});
