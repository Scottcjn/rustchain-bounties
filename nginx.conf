# Add the following lines to the server block for rustchain.org and explorer.rustchain.org
location /api/network {
    try_files $uri $uri/ /api/index.html;
}

location /api/peers {
    try_files $uri $uri/ /api/index.html;
}

location /api/agents {
    try_files $uri $uri/ /api/index.html;
}

location /api/bounties {
    try_files $uri $uri/ /api/index.html;
}