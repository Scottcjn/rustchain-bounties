# ... existing configuration ...

# Add the following block to handle the missing endpoints
location /agent/transactions {
    proxy_pass http://localhost:8080/agent/transactions;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /agent/reputation {
    proxy_pass http://localhost:8080/agent/reputation;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /anchors/recent {
    proxy_pass http://localhost:8080/anchors/recent;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /api/health {
    proxy_pass http://localhost:8080/api/health;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /api/wallet {
    proxy_pass http://localhost:8080/api/wallet;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /beacon/beacons {
    proxy_pass http://localhost:8080/beacon/beacons;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

location /beacon/services {
    proxy_pass http://localhost:8080/beacon/services;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

# ... existing configuration ...