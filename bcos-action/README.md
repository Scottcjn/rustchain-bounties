# BCOS v2 GitHub Action

This GitHub Action allows you to run BCOS v2 scans on your repository. It requires the following inputs:

- `tier`: The tier to be checked (L0, L1, L2)
- `reviewer`: The reviewer assigned to the pull request
- `node_url`: The URL of the RustChain node

## Usage

You can use