# ... existing configuration ...

location /anchors {
    proxy_pass http://explorer.rustchain.org/anchors;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

# ... existing configuration ...