import React from 'react';
import ReactDOM from 'react-dom/client';
import { AtlasCity } from './AtlasCity';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AtlasCity />
  </React.StrictMode>
);