[package]
name = "rustchain-miner"
version = "1.0.0"
edition = "2021"
authors = ["RustChain Team"]
description = "RustChain miner with dry-run capability"

[dependencies]
clap = { version = "4.0", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
panic = 'abort'
